gem 'pivotal-tracker', '>= 0.0.6'
gem 'builder' # until pivotal-tracker gets updated
gem 'ruport'
gem 'git'

gem 'leftright',  :only => :test
gem 'rr',         :only => :test
gem 'shoulda',    :only => :test

bin_path "gbin"
